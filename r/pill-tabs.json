{"$schema":"https://ui.shadcn.com/schema/registry-item.json","name":"pill-tabs","type":"registry:component","title":"Pill Tabs","description":"A segmented tab control with a sliding active state.","dependencies":["framer-motion"],"files":[{"path":"components/ui/PillTabs.tsx","type":"registry:component","content":"\"use client\";\n\nimport { useRef, useState } from \"react\";\nimport { motion, useReducedMotion } from \"framer-motion\";\n\nexport function PillTabs({ tabs }: { tabs: string[] }) {\n  const [active, setActive] = useState(0);\n  const tabRefs = useRef<(HTMLButtonElement | null)[]>([]);\n  const shouldReduceMotion = useReducedMotion();\n\n  const focusTab = (index: number) => {\n    setActive(index);\n    tabRefs.current[index]?.focus();\n  };\n\n  const handleKeyDown = (e: React.KeyboardEvent) => {\n    if (e.key === \"ArrowRight\") {\n      e.preventDefault();\n      focusTab((active + 1) % tabs.length);\n    } else if (e.key === \"ArrowLeft\") {\n      e.preventDefault();\n      focusTab((active - 1 + tabs.length) % tabs.length);\n    } else if (e.key === \"Home\") {\n      e.preventDefault();\n      focusTab(0);\n    } else if (e.key === \"End\") {\n      e.preventDefault();\n      focusTab(tabs.length - 1);\n    }\n  };\n\n  return (\n    <div>\n      <div\n        role=\"tablist\"\n        aria-label=\"Sections\"\n        onKeyDown={handleKeyDown}\n        className=\"inline-flex gap-1 rounded-full bg-slate-100 p-1 dark:bg-neutral-900\"\n      >\n        {tabs.map((tab, i) => (\n          <button\n            key={tab}\n            ref={(el) => { tabRefs.current[i] = el; }}\n            id={`tab-${i}`}\n            role=\"tab\"\n            aria-selected={active === i}\n            aria-controls={`panel-${i}`}\n            tabIndex={active === i ? 0 : -1}\n            onClick={() => setActive(i)}\n            className={`relative rounded-full px-4 py-1.5 text-sm font-medium transition-colors ${\n              active === i\n                ? \"text-slate-900 dark:text-white\"\n                : \"text-slate-500 hover:text-slate-700 dark:text-slate-400 dark:hover:text-slate-200\"\n            }`}\n          >\n            {active === i && (\n              <motion.span\n                layoutId=\"pill-tabs-active-bg\"\n                className=\"absolute inset-0 rounded-full bg-white shadow-sm dark:bg-black\"\n                transition={\n                  shouldReduceMotion ? { duration: 0 } : { type: \"spring\", stiffness: 500, damping: 35 }\n                }\n              />\n            )}\n            <span className=\"relative\">{tab}</span>\n          </button>\n        ))}\n      </div>\n      {tabs.map((tab, i) => (\n        <div\n          key={tab}\n          id={`panel-${i}`}\n          role=\"tabpanel\"\n          aria-labelledby={`tab-${i}`}\n          hidden={active !== i}\n          className=\"pt-3 text-sm text-slate-500 dark:text-slate-400\"\n        >\n          {tab} content goes here.\n        </div>\n      ))}\n    </div>\n  );\n}","target":"components/ui/PillTabs.tsx"}]}