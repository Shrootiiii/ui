{"$schema":"https://ui.shadcn.com/schema/registry-item.json","name":"share-sheet","type":"registry:component","title":"Share Sheet","description":"The dialog that produces that card: a sliding Only you / Anyone with the link toggle reveals a real clipboard-copy link row (the button icon morphs link-to-check) and a live preview of the resulting share card, so the two pieces are shown as one flow.","dependencies":["framer-motion","@phosphor-icons/react"],"files":[{"path":"components/ui/ShareSheet.tsx","type":"registry:component","content":"\"use client\";\n\nimport { useState } from \"react\";\nimport { AnimatePresence, motion, useReducedMotion } from \"framer-motion\";\nimport { LinkIcon as PhLink, CheckIcon as PhCheck } from \"@phosphor-icons/react\";\n\ntype Visibility = \"private\" | \"anyone\";\n\nfunction LinkIcon() {\n  return <PhLink size={14} aria-hidden />;\n}\n\nfunction CheckIcon() {\n  return <PhCheck size={14} weight=\"bold\" aria-hidden />;\n}\n\nexport function ShareSheet() {\n  const [visibility, setVisibility] = useState<Visibility>(\"anyone\");\n  const [copied, setCopied] = useState(false);\n  const shouldReduceMotion = useReducedMotion();\n  const shareUrl = \"claude.ai/share/8f2a-91c4\";\n\n  async function handleCopy() {\n    try {\n      await navigator.clipboard.writeText(`https://${shareUrl}`);\n    } catch {\n      // clipboard permission denied; UI still confirms optimistically\n    }\n    setCopied(true);\n    window.setTimeout(() => setCopied(false), 2000);\n  }\n\n  return (\n    <div className=\"w-full max-w-sm rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-700 dark:bg-neutral-950\">\n      <h3 className=\"text-sm font-semibold text-slate-900 dark:text-white\">Share this chat</h3>\n      <p className=\"mt-1 text-xs leading-relaxed text-slate-500 dark:text-slate-400\">\n        Anyone with the link can view this conversation. It won't update after you share it.\n      </p>\n\n      <div className=\"relative mt-4 grid grid-cols-2 rounded-lg bg-slate-100 p-1 text-sm font-medium dark:bg-neutral-900\">\n        {([\"private\", \"anyone\"] as const).map((option) => (\n          <button\n            key={option}\n            type=\"button\"\n            onClick={() => setVisibility(option)}\n            className={`relative z-10 rounded-md px-3 py-1.5 transition-colors ${\n              visibility === option ? \"text-slate-900 dark:text-white\" : \"text-slate-500 dark:text-slate-400\"\n            }`}\n          >\n            {visibility === option && (\n              <motion.span\n                layoutId=\"share-visibility-highlight\"\n                className=\"absolute inset-0 -z-10 rounded-md bg-white shadow-sm dark:bg-neutral-700\"\n                transition={shouldReduceMotion ? { duration: 0 } : { type: \"spring\", stiffness: 500, damping: 34 }}\n              />\n            )}\n            {option === \"private\" ? \"Only you\" : \"Anyone with link\"}\n          </button>\n        ))}\n      </div>\n\n      <AnimatePresence initial={false}>\n        {visibility === \"anyone\" && (\n          <motion.div\n            initial={shouldReduceMotion ? { opacity: 1, height: \"auto\" } : { opacity: 0, height: 0 }}\n            animate={{ opacity: 1, height: \"auto\" }}\n            exit={shouldReduceMotion ? { opacity: 0 } : { opacity: 0, height: 0 }}\n            transition={{ duration: 0.3, ease: [0.22, 1, 0.36, 1] }}\n            className=\"overflow-hidden\"\n          >\n            <div className=\"mt-4 flex items-center gap-2 rounded-lg border border-slate-200 bg-slate-50 py-1 pl-3 pr-1 dark:border-slate-800 dark:bg-neutral-900\">\n              <span className=\"min-w-0 flex-1 truncate text-xs text-slate-500 dark:text-slate-400\">{shareUrl}</span>\n              <button\n                type=\"button\"\n                onClick={handleCopy}\n                className={`inline-flex shrink-0 items-center gap-1.5 rounded-md px-2.5 py-1.5 text-xs font-medium transition-colors duration-200 ${\n                  copied\n                    ? \"bg-emerald-100 text-emerald-700 dark:bg-emerald-500/15 dark:text-emerald-400\"\n                    : \"bg-slate-900 text-white hover:bg-slate-700 dark:bg-white dark:text-slate-900 dark:hover:bg-slate-200\"\n                }`}\n              >\n                <AnimatePresence mode=\"wait\" initial={false}>\n                  <motion.span\n                    key={copied ? \"check\" : \"link\"}\n                    initial={{ opacity: 0, scale: 0.6 }}\n                    animate={{ opacity: 1, scale: 1 }}\n                    exit={{ opacity: 0, scale: 0.6 }}\n                    transition={{ duration: 0.15 }}\n                    className=\"inline-flex\"\n                  >\n                    {copied ? <CheckIcon /> : <LinkIcon />}\n                  </motion.span>\n                </AnimatePresence>\n                {copied ? \"Copied\" : \"Copy link\"}\n              </button>\n            </div>\n\n            <p className=\"mt-3 text-[11px] font-medium uppercase tracking-wide text-slate-400 dark:text-slate-500\">\n              What people will see\n            </p>\n            <div className=\"mt-2 rounded-xl border border-slate-200 bg-slate-50 p-3 dark:border-slate-800 dark:bg-neutral-900\">\n              <div className=\"flex items-center gap-2\">\n                <span className=\"h-1.5 w-1.5 rounded-full bg-amber-500\" aria-hidden />\n                <span className=\"text-[11px] font-medium uppercase tracking-wide text-slate-500 dark:text-slate-400\">\n                  Shared from Claude\n                </span>\n              </div>\n              <p className=\"mt-1.5 truncate text-sm font-medium text-slate-900 dark:text-white\">\n                Debugging a flaky CI pipeline\n              </p>\n            </div>\n          </motion.div>\n        )}\n      </AnimatePresence>\n    </div>\n  );\n}","target":"components/ui/ShareSheet.tsx"}]}