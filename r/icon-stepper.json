{"$schema":"https://ui.shadcn.com/schema/registry-item.json","name":"icon-stepper","type":"registry:component","title":"Icon Stepper","description":"A stepper with a swappable per-step icon instead of a number: the demo's own icon set swap button proves it, live.","dependencies":["framer-motion"],"files":[{"path":"components/ui/IconStepper.tsx","type":"registry:component","content":"\"use client\";\n\nimport { useState } from \"react\";\nimport { AnimatePresence, motion, useReducedMotion } from \"framer-motion\";\n\nexport function IconStepper({\n  steps,\n  initial = 0,\n}: {\n  steps: { label: string; icon: React.ReactNode }[];\n  initial?: number;\n}) {\n  const [current, setCurrent] = useState(initial);\n  const shouldReduceMotion = useReducedMotion();\n\n  return (\n    <ol className=\"flex w-full items-center\">\n      {steps.map((step, i) => {\n        const isComplete = i < current;\n        const isActive = i === current;\n        return (\n          <li key={step.label} className=\"flex flex-1 items-center last:flex-none\">\n            <button\n              type=\"button\"\n              onClick={() => setCurrent(i)}\n              aria-current={isActive ? \"step\" : undefined}\n              className=\"flex flex-col items-center gap-2 rounded-full focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-amber-500\"\n            >\n              <motion.span\n                animate={isActive && !shouldReduceMotion ? { scale: [1, 1.15, 1] } : { scale: 1 }}\n                transition={{ duration: 0.5, ease: [0.16, 1, 0.3, 1] }}\n                className={`relative flex h-8 w-8 shrink-0 items-center justify-center rounded-full border-2 transition-colors duration-300 ${\n                  isComplete\n                    ? \"border-amber-600 bg-amber-600 text-white\"\n                    : isActive\n                      ? \"border-amber-600 text-amber-600 dark:text-amber-400\"\n                      : \"border-slate-300 text-slate-400 dark:border-slate-600 dark:text-slate-500\"\n                }`}\n              >\n                <AnimatePresence initial={false} mode=\"wait\">\n                  {isComplete ? (\n                    <motion.svg\n                      key=\"check\"\n                      viewBox=\"0 0 16 16\"\n                      width=\"14\"\n                      height=\"14\"\n                      fill=\"none\"\n                      stroke=\"currentColor\"\n                      strokeWidth=\"2.5\"\n                      strokeLinecap=\"round\"\n                      strokeLinejoin=\"round\"\n                      aria-hidden\n                      initial={{ opacity: 0, scale: 0.5 }}\n                      animate={{ opacity: 1, scale: 1 }}\n                      exit={{ opacity: 0, scale: 0.5 }}\n                      transition={shouldReduceMotion ? { duration: 0 } : { duration: 0.25, ease: [0.22, 1, 0.36, 1] }}\n                    >\n                      <path d=\"M3 8.5l3 3 7-7.5\" />\n                    </motion.svg>\n                  ) : (\n                    <motion.span\n                      key=\"icon\"\n                      initial={{ opacity: 0, scale: 0.5, rotate: -20 }}\n                      animate={{ opacity: 1, scale: 1, rotate: 0 }}\n                      exit={{ opacity: 0, scale: 0.5, rotate: 20 }}\n                      transition={shouldReduceMotion ? { duration: 0 } : { duration: 0.25, ease: [0.22, 1, 0.36, 1] }}\n                      className=\"flex items-center justify-center\"\n                    >\n                      {step.icon}\n                    </motion.span>\n                  )}\n                </AnimatePresence>\n              </motion.span>\n              <span\n                className={`text-xs font-medium ${\n                  isActive ? \"text-slate-900 dark:text-white\" : \"text-slate-400 dark:text-slate-500\"\n                }`}\n              >\n                {step.label}\n              </span>\n            </button>\n            {i < steps.length - 1 && (\n              <div className=\"mx-2 mb-5 h-0.5 flex-1 overflow-hidden rounded-full bg-slate-200 dark:bg-slate-700\">\n                <motion.div\n                  className=\"h-full bg-amber-600\"\n                  initial={false}\n                  animate={{ scaleX: isComplete ? 1 : 0 }}\n                  style={{ transformOrigin: \"left\" }}\n                  transition={shouldReduceMotion ? { duration: 0 } : { duration: 0.4, ease: [0.22, 1, 0.36, 1] }}\n                />\n              </div>\n            )}\n          </li>\n        );\n      })}\n    </ol>\n  );\n}","target":"components/ui/IconStepper.tsx"}]}