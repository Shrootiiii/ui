{"$schema":"https://ui.shadcn.com/schema/registry-item.json","name":"toast","type":"registry:component","title":"Toast","description":"A corner notification with a draining progress bar and drag-to-dismiss; swipe it away or let it time out.","dependencies":["framer-motion","@phosphor-icons/react"],"files":[{"path":"components/ui/Toast.tsx","type":"registry:component","content":"\"use client\";\n\nimport { useCallback, useEffect, useRef, useState } from \"react\";\nimport { AnimatePresence, motion, useReducedMotion } from \"framer-motion\";\nimport { CheckIcon as PhCheck } from \"@phosphor-icons/react\";\n\nconst DURATION = 4000;\n\nexport function useToast() {\n  const [visible, setVisible] = useState(false);\n  const [key, setKey] = useState(0);\n  const timerRef = useRef<ReturnType<typeof setTimeout> | undefined>(undefined);\n\n  const dismiss = useCallback(() => {\n    setVisible(false);\n    clearTimeout(timerRef.current);\n  }, []);\n\n  const show = useCallback(() => {\n    setKey((k) => k + 1);\n    setVisible(true);\n    clearTimeout(timerRef.current);\n    timerRef.current = setTimeout(() => setVisible(false), DURATION);\n  }, []);\n\n  useEffect(() => () => clearTimeout(timerRef.current), []);\n\n  return { visible, key, show, dismiss };\n}\n\nexport function Toast({\n  visible,\n  onDismiss,\n  title,\n  description,\n}: {\n  visible: boolean;\n  onDismiss: () => void;\n  title: string;\n  description: string;\n}) {\n  const shouldReduceMotion = useReducedMotion();\n\n  return (\n    <div className=\"pointer-events-none fixed right-4 top-4 z-50\">\n      <AnimatePresence>\n        {visible && (\n          <motion.div\n            role=\"status\"\n            drag=\"x\"\n            dragConstraints={{ left: 0, right: 0 }}\n            dragElastic={{ left: 0, right: 0.5 }}\n            onDragEnd={(_, info) => {\n              if (info.offset.x > 60) onDismiss();\n            }}\n            initial={shouldReduceMotion ? { opacity: 0 } : { opacity: 0, x: 40, scale: 0.95 }}\n            animate={{ opacity: 1, x: 0, scale: 1 }}\n            exit={shouldReduceMotion ? { opacity: 0 } : { opacity: 0, x: 60, scale: 0.95 }}\n            transition={{ type: \"spring\", stiffness: 420, damping: 32 }}\n            className=\"pointer-events-auto relative w-64 cursor-grab overflow-hidden rounded-xl border border-slate-200 bg-white p-3.5 shadow-lg active:cursor-grabbing dark:border-slate-700 dark:bg-neutral-950\"\n          >\n            <div className=\"flex items-start gap-2.5\">\n              <span className=\"mt-0.5 flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-emerald-100 text-emerald-700 dark:bg-emerald-500/15 dark:text-emerald-400\">\n                <PhCheck size={14} weight=\"bold\" aria-hidden />\n              </span>\n              <div className=\"flex-1\">\n                <p className=\"text-sm font-medium text-slate-900 dark:text-white\">{title}</p>\n                <p className=\"text-xs text-slate-500 dark:text-slate-400\">{description}</p>\n              </div>\n            </div>\n            <div className=\"absolute inset-x-0 bottom-0 h-0.5 bg-slate-100 dark:bg-neutral-900\">\n              <motion.div\n                className=\"h-full bg-emerald-500\"\n                initial={{ scaleX: 1 }}\n                animate={{ scaleX: 0 }}\n                style={{ transformOrigin: \"left\" }}\n                transition={shouldReduceMotion ? { duration: 0 } : { duration: DURATION / 1000, ease: \"linear\" }}\n              />\n            </div>\n          </motion.div>\n        )}\n      </AnimatePresence>\n    </div>\n  );\n}\n\n// Usage: const toast = useToast(); <button onClick={toast.show}>Show</button>\n// <Toast visible={toast.visible} onDismiss={toast.dismiss} title=\"Changes saved\" description=\"...\" />","target":"components/ui/Toast.tsx"}]}